name: 'EcoCompute Energy Auditor'
description: 'GitHub Marketplace Bot — Auto-review PRs for GPU/LLM energy waste. 93+ real GPU measurements. Zero config.'
inputs:
  gemini_key:
    description: 'Your Gemini API Key'
    required: true
  github_token:
    description: 'GitHub Token for PR comments'
    required: true
runs:
  using: 'node20'
  main: 'action-dist/index.js'
